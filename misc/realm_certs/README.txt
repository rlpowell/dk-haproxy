Generated using https://github.com/FiloSottile/mkcert

Slight special case is that we stuck the binary in ~/bin/

FWIW, I installed it on Windows like so:

# certutil -f -user -addstore Root Downloads\realm_ca_cert.pem
